const heading = document.getElementById('heading');

function small() {
    heading.className = ' ';
    heading.classList.add('small');
}

function middle() {
    heading.className = ' ';
    heading.classList.add('middle');
}

function big() {
    heading.className = ' ';
    heading.classList.add('big');
}
